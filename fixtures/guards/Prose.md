# Core guards

The daemon fixed the bug. The tests are green and the build is clean.

The bug has been fixed. The file is being written. The config was written by
hand.

Note: this behavior is documented.

Tip: the cache warms on first read.

Ordering: the writer holds the lock.

```console
$ grep "Why it matters:" notes.txt
```
